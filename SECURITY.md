# 安全说明

## 支持范围

当前安全维护范围是 Android `1.0.x` 源码，当前公共源码版本为 `1.0.1`（`versionCode=4`）。iOS 尚未开始，Web 也未完成产品验收。第一阶段公共仓库不发布 APK 或 AAB。

## 报告安全问题

公开仓库建立前尚未配置已验证的私密漏洞报告渠道。此期间如需联系维护者，请创建一个标题为 `Security contact request` 的普通 Issue；Issue 中只能请求私密联系方式，不得包含漏洞细节、利用步骤、日志、附件或个人数据。维护者会提供后续私密沟通方式。

普通 Issue 不应用来公开尚未修复的漏洞。未来只有在仓库实际启用 GitHub Private Vulnerability Reporting 并更新本文件后，才应直接使用该功能提交详情。

私密报告建议包含：

- 受影响的版本、Android 版本和设备类别。
- 最小化、可重复的复现步骤和预期/实际结果。
- 已去除个人信息的日志或示例。
- 对数据完整性、机密性或可用性的影响判断。

## 不要提交的内容

请勿提交真实宝宝资料、真实照片、数据库、CSV、ZIP 备份、设备序列号、访问令牌、API Key、私钥、JKS/keystore、`key.properties`、密码或签名材料。不要为了复现问题上传含家庭数据的完整设备日志。

如需示例数据，请使用虚构资料并先移除文件元数据。日志应只保留与复现有关的最小片段，同时删除本机路径、用户名、设备标识和联系方式。

## 依赖与构建

- 使用 `npm ci` 按 `package-lock.json` 安装精确依赖。
- 不要在 Pull Request 中加入 APK、AAB、签名材料或本地构建产物。
- 普通 Android development build 不需要 release keystore。
- 发现第三方依赖漏洞时，请同时说明它是否能从本项目实际代码路径触达。

安全修复可能在公开说明前先进行协调披露。维护者不会要求报告者提供真实家庭数据或签名密钥。
